# Optional project-scoped Codex config snippet.
# Copy into .codex/config.toml only if your team wants these subagent limits.
# Keep max_depth at 1 to avoid uncontrolled recursive delegation.

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 36000
